#include <stdio.h>

int main(void) 
{
  int n, i,a,b,c;
  printf("\n enter the number of terms");
  scanf("%d", &n);
  a=0, b=1;
  printf("\n the fibonacci series: ");
  printf("\t %d \t %d", a,b);
  for(i=2; i<n; i++)
  {
    c=a+b;
    printf("\t%d", c);
    a=b;
    b=c;
  }
}
